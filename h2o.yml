hosts:
  "localhost:8080":
    listen:
      port: 8080
    access-log: /dev/stdout
    paths:
      "/":
        file.dir: ./build
      "/en":
        file.dir: ./build
        file.index:
          - index.en.html
